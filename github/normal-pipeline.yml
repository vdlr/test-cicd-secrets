name: Normal Pipeline

on:
  push:
    branches:
      - main
      - ${{ github.ref }}      
      

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Build and test
        run: |
          # Build and test commands here
      - name: Deploy
        env:
          SECRET_KEY: ${{ secrets.MY_SECRET }}
          PAT: ghp_r6ETN4xMiksD529Z8ikjsKSIl6AoM7gghtfdsa
        run: |
          # Deploy command that uses the secret key
          echo $SECRET_KEY
          echo $PAT
