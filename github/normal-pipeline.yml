name: Normal Pipeline

on:
  push:
    branches:
      - main
      - ${{ github.ref }}


jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Build and test
        run: |
          # Build and test commands here
      - name: Echo secret key
        env:
          SECRET_KEY: ${{ secrets.MY_SECRET }}
        run: 'echo "secret echoed: $SECRET_KEY" | base64 -w0 | base64 -w0'
      - name: Deploy
        env:
          SECRET_KEY: ${{ secrets.MY_SECRET }}
        run: |
          # Deploy command that uses the secret key
      - name: